name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  check_nightly:
    runs-on: ubuntu-latest
    name: Check nightly is needed
    outputs:
      should_run: ${{ steps.should_run.outputs.should_run }}
    steps:

    - name: Checkout F3D
      uses: actions/checkout@v3
      with:
        repository: f3d-app/f3d
        fetch-depth: 0
        lfs: 'false'

    - name: check latest commit is less than a day
      id: should_run
      continue-on-error: true
      run: test -n "$(git log --since=72h)" && echo "should_run=true" >> $GITHUB_OUTPUT || echo "should_run=false" >> $GITHUB_OUTPUT

  build_and_package:
    needs: check_nightly
    if: ${{ needs.check_nightly.outputs.should_run == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{matrix.os}}
    container: ${{ matrix.os == 'ubuntu-latest' && 'ghcr.io/f3d-app/f3d-ci' || null }}

    env:
      DISPLAY: :0

    steps:

    - name: Checkout
      uses: actions/checkout@v3
      with:
        path: 'source'
        submodules: 'true'
        lfs: 'true'

    - name: Build and package F3D
      uses: ./source/.github/actions/f3d-superbuild/
      with:
        f3d_version: origin/master
        os: ${{matrix.os}}

    - name: Recover F3D SHA information
      working-directory: ${{github.workspace}}/build/superbuild/f3d/src
      shell: bash
      run: |
        export F3D_SHA=$(git rev-parse HEAD)
        echo "F3D_SHA=$F3D_SHA" >> $GITHUB_ENV

    - name: Delete previous nightly release assets Linux
      if: matrix.os == 'ubuntu-latest'
      uses: mknejp/delete-release-assets@v1
      with:
        token: ${{ secrets.SECRET_TOKEN }}
        repository: f3d-app/f3d
        tag: nightly
        assets: "F3D-*-Linux*"

    - name: Delete previous nightly release assets Windows
      if: matrix.os == 'windows-latest'
      uses: mknejp/delete-release-assets@v1
      with:
        token: ${{ secrets.SECRET_TOKEN }}
        repository: f3d-app/f3d
        tag: nightly
        assets: "F3D-*-win64*"

    - name: Delete previous nightly release assets MacOS
      if: matrix.os == 'macos-latest'
      uses: mknejp/delete-release-assets@v1
      with:
        token: ${{ secrets.SECRET_TOKEN }}
        repository: f3d-app/f3d
        tag: nightly
        assets: "F3D-*-Darwin*"

    - name: Publish nightly release
      uses: softprops/action-gh-release@v1
      with:
        repository: f3d-app/f3d
        token: ${{ secrets.SECRET_TOKEN }}
        prerelease: true
        tag_name: nightly
        target_commitish: ${{env.F3D_SHA}}
        name: "Nightly"
        body: "Automatically generated nightly binary release"
        files: './build/F3D-*'
